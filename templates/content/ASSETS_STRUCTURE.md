# Assets Directory Structure

このドキュメントは、コンテンツ制作に関連するアセット（サムネイル画像等）のディレクトリ構造を定義します。

## 📁 Standard Directory Structure

```
content/
├── plans/                          # コンテンツ企画書
│   └── [ID]-[シリーズ名]-plan.md
├── schedules/                      # 投稿スケジュール
│   └── [ID]-[シリーズ名]-schedule.md
├── articles/                       # 記事ファイル
│   ├── [ID]-[記事番号]-[記事名].md
│   └── ...
└── assets/                         # ⭐ 新規追加
    └── thumbnails/                 # サムネイル画像
        ├── [ID]-[記事番号]-thumbnail.png
        └── ...
```

または、プロジェクト別に整理する場合：

```
specs/contents/[project]/
├── plans/
├── schedules/
├── articles/
└── assets/
    └── thumbnails/
```

## 🎨 Thumbnails Directory

### 目的
note記事用のサムネイル（OGP画像）を保存する専用ディレクトリ

### 自動生成タイミング
- **canva-thumbnail-generator**エージェントによって自動生成
- content-executor Phase 2完了後に実行
- content-reviewer実行前に配置完了

### ファイル命名規則

#### 基本パターン
```
[記事ID]-thumbnail.png
```

**例**:
- `C001-thumbnail.png`
- `C002-thumbnail.png`

#### 詳細パターン（記事番号付き）
```
[記事ID]-[記事番号]-thumbnail.png
```

**例**:
- `C001-01-thumbnail.png` （シリーズ第1記事）
- `C001-02-thumbnail.png` （シリーズ第2記事）

### 技術仕様

**画像フォーマット**:
- **推奨**: PNG（透過なし）
- **代替**: JPG（品質90%以上）
- **サイズ**: 1280x670px（OGP標準）
- **アスペクト比**: 1.91:1
- **ファイルサイズ**: 1MB以下

**メタデータ統合**:
記事ファイルのメタデータセクションに自動追加：
```markdown
- **サムネイル**: assets/thumbnails/[ID]-thumbnail.png
- **サムネイル生成日**: YYYY-MM-DD
- **Canvaデザイン**: https://www.canva.com/design/...
```

## 🔄 Directory Creation Flow

### 自動作成
**canva-thumbnail-generator**エージェントが実行時に以下を確認：

1. `content/assets/thumbnails/` ディレクトリの存在確認
2. 存在しない場合は自動作成
3. サムネイル画像を保存
4. 記事メタデータを更新

### 手動作成（初回セットアップ）
プロジェクト初期化時に以下のコマンドで作成可能：

```bash
# ルートディレクトリから実行
mkdir -p content/assets/thumbnails

# プロジェクト別の場合
mkdir -p specs/contents/[project]/assets/thumbnails
```

## 🗂️ File Management

### バージョン管理
- サムネイル画像ファイルはGit管理対象
- `.gitignore`で除外しない（コンテンツの一部として管理）

### 命名衝突回避
- 記事IDは一意なため、衝突リスクは低い
- 上書き前に既存ファイル確認（オプション）

### クリーンアップ
- 記事削除時に対応するサムネイルも削除推奨
- 孤立したサムネイルファイルの定期確認

## 🎯 Integration with Agents

### canva-thumbnail-generator
**責務**:
- ディレクトリの存在確認・作成
- サムネイル画像の生成・保存
- 記事メタデータの更新

**保存パス決定ロジック**:
```
1. 記事ファイルパスを取得
   例: content/articles/C001-01-article.md

2. プロジェクトルートを特定
   例: content/

3. assetsパスを構築
   例: content/assets/thumbnails/

4. サムネイルファイル名を決定
   例: C001-01-thumbnail.png

5. 完全パスを生成
   例: content/assets/thumbnails/C001-01-thumbnail.png
```

### content-executor
**責務**:
- サムネイル生成の必要性判定
- 構造化レスポンスでサムネイル生成要求
- 記事メタデータにサムネイルパス記載

### content-reviewer
**責務**:
- サムネイル存在確認
- サムネイルと記事内容の整合性チェック
- サムネイル品質基準確認

## 📝 Example Structure

実際のプロジェクト例：

```
content/
├── plans/
│   └── C001-side-business-plan.md
├── schedules/
│   └── C001-side-business-schedule.md
├── articles/
│   ├── C001-01-introduction.md
│   ├── C001-02-skills.md
│   └── C001-03-monetization.md
└── assets/
    └── thumbnails/
        ├── C001-01-thumbnail.png    # 890KB, 1280x670px
        ├── C001-02-thumbnail.png    # 920KB, 1280x670px
        └── C001-03-thumbnail.png    # 875KB, 1280x670px
```

## 🚨 Error Handling

### ディレクトリ作成失敗
```json
{
  "status": "error",
  "errorType": "directory_creation_failed",
  "errorMessage": "assets/thumbnailsディレクトリの作成に失敗",
  "recommendation": "ディスク容量と権限を確認してください"
}
```

### 保存パス解決失敗
```json
{
  "status": "error",
  "errorType": "path_resolution_failed",
  "errorMessage": "記事ファイルからプロジェクトルートを特定できません",
  "recommendation": "記事ファイルパスが標準構造に準拠しているか確認"
}
```

## 🔐 Security Considerations

### ファイル権限
- 生成されたサムネイルは適切な読み取り権限を設定
- 書き込み権限は最小限に制限

### パストラバーサル対策
- ファイルパスの検証を実施
- `../`等の相対パス表記を禁止
- 許可されたディレクトリ内のみに保存

---

**Note**: このディレクトリ構造は、ai-spec-noteフレームワークのコンテンツ制作フローに統合されています。canva-thumbnail-generatorエージェントが自動的にディレクトリを管理します。
